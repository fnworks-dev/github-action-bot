name: SideQuest Bot (Legacy)

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    if: github.repository == 'fnworks-dev/github-action-bot'
    runs-on: ubuntu-latest

    steps:
      - name: Legacy Workflow Notice
        run: |
          echo "This legacy SideQuest workflow is intentionally disabled."
          echo "Use sidequest-bot-01.yml through sidequest-bot-04.yml."
